{
  "name": "thinkingdata-mcp",
  "version": "0.1.0",
  "description": "Read-only MCP server for ThinkingData 5.0",
  "type": "module",
  "bin": {
    "thinkingdata-readonly": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "7.0.2"
  }
}
